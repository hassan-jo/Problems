#include <iostream>
using namespace std;

int main() {
	long long int n1, n2, n3;
	cin >> n1 >> n2 >> n3;
	if (n1 < (n2 + n3) && n2 < (n1 + n3) && n3 < (n2 + n1))
	{
		cout << "YES";
	}
	else
	{
		cout << "NO";
	}
	return 0;
}
